
"use client"

import AwardCoinsPage from "@/app/coinseller/award/page"

// Re-using the same UI but the logic inside AwardCoinsPage handles isAdmin vs isCoinseller
export default function AdminAwardPage() {
  return <AwardCoinsPage />
}
