
{
  "rules": {
    "users": {
      ".read": "auth != null",
      "$uid": {
        ".write": "auth.uid === $uid",
        "presence": {
          "online": { ".validate": "newData.isBoolean()" },
          "lastSeen": { ".validate": "newData.isNumber()" }
        },
        "coinBalance": {
          ".validate": "newData.isNumber()"
        },
        "diamondBalance": {
          // Allows other users to increase this value when sending a gift via transaction
          ".write": "auth != null",
          ".validate": "newData.isNumber()"
        },
        "inCall": {
          ".validate": "newData.isBoolean()"
        },
        "chats": {
          "$otherId": {
            // Allows the sender (otherId) to update unreadCount on the receiver profile
            ".write": "auth.uid === $uid || auth.uid === $otherId",
            "unreadCount": {
               ".validate": "newData.isNumber() && newData >= 0"
            }
          }
        }
      }
    },
    "chats": {
      "$chatId": {
        ".read": "auth != null && $chatId.contains(auth.uid)",
        ".write": "auth != null && $chatId.contains(auth.uid)",
        "messages": {
          "$msgId": {
            ".validate": "newData.hasChildren(['messageText', 'senderId', 'sentAt'])",
            "senderId": { ".validate": "newData.val() === auth.uid" },
            "sentAt": { ".validate": "newData.isNumber()" }
          }
        }
      }
    },
    "calls": {
      "$chatId": {
        ".read": "auth != null && $chatId.contains(auth.uid)",
        ".write": "auth != null && $chatId.contains(auth.uid)"
      }
    }
  }
}
